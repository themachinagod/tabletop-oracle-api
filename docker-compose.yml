services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: tabletop_oracle
      POSTGRES_USER: tabletop
      POSTGRES_PASSWORD: tabletop_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
