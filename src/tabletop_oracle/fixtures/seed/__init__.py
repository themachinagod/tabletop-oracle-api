"""Seed fixture data for bootstrapping a fresh deployment.

Contains JSON metadata files for users, games, expansions, documents,
AI configuration, and ground truth Q&A pairs. The manifest.json file
declares all seed contents and their dependency ordering.

PDF document files are added separately (task #85).
"""
